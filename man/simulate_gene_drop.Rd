% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-gene-drop.R
\name{simulate_gene_drop}
\alias{simulate_gene_drop}
\title{Simulate gene dropping through a pedigree}
\usage{
simulate_gene_drop(
  base_haplotypes,
  map,
  generations,
  population_size,
  selected_parents,
  offspring_per_mating,
  sample_size = population_size,
  seed = NULL,
  keep = c("sample", "all")
)
}
\arguments{
\item{base_haplotypes}{A list containing \code{hap1} and \code{hap2}, both 0/1
matrices with individuals in rows and markers in columns.}

\item{map}{Numeric vector of marker positions in Morgans. Positions must be
finite, non-missing, non-decreasing, and span a positive chromosome
length.}

\item{generations}{Number of generations to simulate after generation 0.}

\item{population_size}{Number of offspring in each simulated generation.}

\item{selected_parents}{Total number of parents selected from each previous
generation. At least two parents are required.}

\item{offspring_per_mating}{Number of offspring generated per mating.}

\item{sample_size}{Number of individuals sampled from the final generation.}

\item{seed}{Optional non-negative integer random seed.}

\item{keep}{Whether returned haplotypes should contain only sampled
individuals (\code{"sample"}) or all simulated individuals (\code{"all"}).}
}
\value{
An object of class \code{qgsim_gene_drop}, containing the pedigree,
retained haplotypes, sampled individuals, map, and simulation parameters.
}
\description{
Simulates a simple single-chromosome gene-dropping example. The supplied
base haplotypes form generation 0. In each subsequent generation, selected
parents produce offspring whose paternal and maternal haplotypes are formed
by recombination.
}
\details{
This function is intended for teaching and small examples. It is not a
scientifically complete breeding simulator.
}
\examples{
base <- list(
  hap1 = matrix(c(0, 0, 1, 1, 0, 1, 0, 1), nrow = 4, byrow = TRUE),
  hap2 = matrix(c(1, 1, 0, 0, 1, 0, 1, 0), nrow = 4, byrow = TRUE)
)

result <- simulate_gene_drop(
  base_haplotypes = base,
  map = c(0, 0.5),
  generations = 2,
  population_size = 4,
  selected_parents = 4,
  offspring_per_mating = 2,
  sample_size = 2,
  seed = 123
)
}
