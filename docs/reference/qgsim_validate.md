# Validate a compiled qgsim plan

Validate a compiled qgsim plan

## Usage

``` r
qgsim_validate(plan)
```

## Arguments

- plan:

  A `qgsim_plan`.

## Value

Invisibly returns `TRUE` when validation succeeds.
