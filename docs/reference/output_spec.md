# Define requested simulation outputs

Define requested simulation outputs

## Usage

``` r
output_spec(populations = TRUE, metrics = "mean_breeding_value")
```

## Arguments

- populations:

  Whether to return individual-level population records.

- metrics:

  Character vector of requested generation-level metrics.

## Value

An object of class `qgsim_outputs`.
